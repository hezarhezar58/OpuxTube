<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".OpuxApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OpuxTube"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="34">

        <!-- Sentry crash reporting. The DSN is a public client identifier by design;
             rate-limiting / sampling is enforced server-side per project. We leave
             traces.sample-rate and session-replay rates at their defaults (off) —
             only crashes and explicit Sentry.captureException calls are sent, which
             keeps us inside the free tier with room to spare. -->
        <meta-data
            android:name="io.sentry.dsn"
            android:value="https://042d8b0d17940961a5c0bd134bea9f8a@o4511293190111232.ingest.us.sentry.io/4511293194895360" />
        <meta-data android:name="io.sentry.auto-init" android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:supportsPictureInPicture="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|keyboardHidden"
            android:theme="@style/Theme.OpuxTube.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="dev.opux.tubeclient.core.player.service.OpuxPlaybackService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <service
            android:name="dev.opux.tubeclient.download.OpuxDownloadService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- AppCompatDelegate.setApplicationLocales persists the user's locale via this
             service when autoStoreLocales is true. We don't use it as a real service —
             it's a marker AppCompat reads through PackageManager. -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

    </application>

</manifest>
